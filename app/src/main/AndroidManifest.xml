<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dinotom.project_koff_ma">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".KoffGlobal"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".business_entities.BusinessEntitiesActivity"
            android:windowSoftInputMode="adjustPan" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_api_key" />

        <activity android:name=".SubcategoryActivity" />
        <activity android:name=".business_entities.FilterSettingsActivity" />
        <activity android:name=".business_entities.FilterLocationSettingsActivity" />
        <activity android:name=".business_entities.BusinessEntityInfoActivity" />
        <activity
            android:name=".business_entities.CommentActivity"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />

        <service
            android:name=".business_entities.FetchAddressIntentService"
            android:exported="false" />

        <activity android:name=".RegistrationActivity"></activity>
    </application>

</manifest>